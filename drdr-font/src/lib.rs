//! drdr-font — DrDrFont, the DrDrOS bitmap font renderer.
//!
//! Phase 1 (later) introduces a hand-drawn bitmap font. This crate will
//! expose `draw_glyph` / `draw_text` functions that plot characters onto
//! a framebuffer surface owned by drdr-ui.

// Phase 1 placeholder — keeps the crate buildable.
pub const VERSION: &str = "0.1.0";
