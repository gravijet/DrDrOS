//! drdr-shell — DrDrShell, the DrDrOS interactive command shell.
//!
//! Reads command lines from the user, parses them, runs them, prints the
//! output. We are NOT wrapping bash — DrDrShell is its own parser and
//! executor written from scratch.
//!
//! Planned (Phase 2): pipes (`a | b`), redirects (`cmd > file`), history,
//! tab-completion, and a small set of built-ins.

fn main() {
    println!("drdr-shell (stub) — Phase 2 implementation pending");
}
